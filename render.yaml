services:
  - type: web
    name: btc-signal-simulator
    runtime: node
    plan: free
    rootDir: longshortbtc
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
